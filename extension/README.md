# AutoDoc AI Chrome Extension

## Описание

Chrome-расширение для записи экрана, микрофона и логирования кликов для AutoDoc AI System.

## Установка

### 1. Создание иконок (если отсутствуют)

```bash
# Убедитесь что установлен Python и Pillow
pip install Pillow

# Создайте иконки
python generate_icons.py
```

Или создайте иконки вручную:
- Поместите PNG файлы в папку `icons/`
- Размеры: 16×16, 48×48, 128×128 пикселей

### 2. Загрузка в Chrome

1. Откройте Chrome и перейдите на `chrome://extensions/`
2. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
3. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
4. Выберите папку `extension/` из проекта
5. Расширение появится в списке
6. Закрепите расширение на панели инструментов

### 3. Настройка

1. Нажмите на иконку расширения
2. В настройках укажите URL бэкенда: `http://localhost:8000`
3. Нажмите "Сохранить"

## Использование

### Запись экрана

1. Нажмите на иконку расширения
2. Нажмите **"Начать запись"**
3. Разрешите доступ к экрану и микрофону
4. Выполняйте действия и говорите вслух
5. Нажмите **"Остановить запись"**

### Что записывается

- 🎥 Видео экрана (WebM)
- 🎤 Аудио с микрофона (WAV)
- 🖱️ Клики с координатами и таймкодами (JSON)

## Структура файлов

```
extension/
├── manifest.json          # Манифест расширения
├── background.js          # Service Worker (фоновый процесс)
├── content.js             # Content Script (логирование кликов)
├── popup.html             # UI popup
├── popup.js               # Логика popup
├── generate_icons.py      # Скрипт генерации иконок
└── icons/                 # Иконки расширения
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## Разрешения

Расширение запрашивает следующие разрешения:

- `storage` — сохранение настроек
- `tabCapture` — захват вкладки
- `desktopCapture` — захват экрана
- `scripting` — внедрение скриптов
- `activeTab` — доступ к активной вкладке
- `<all_urls>` — работа на всех сайтах

## Отладка

### Просмотр логов

1. Откройте `chrome://extensions/`
2. Найдите AutoDoc AI Recorder
3. Нажмите **"Inspect views: service worker"**
4. Откроется консоль с логами

### Проверка разрешений

1. Откройте `chrome://settings/content`
2. Проверьте разрешения для:
   - Микрофон
   - Камера (для захвата экрана)

### Частые проблемы

**Расширение не загружается:**
- Проверьте что все файлы на месте
- Убедитесь что иконки созданы
- Проверьте manifest.json на ошибки

**Запись не начинается:**
- Разрешите доступ к микрофону и экрану
- Проверьте URL бэкенда в настройках
- Проверьте консоль расширения

**Клики не логируются:**
- Перезагрузите страницу
- Проверьте что content.js загружен
- Откройте консоль браузера (F12)

## Разработка

### Изменение кода

1. Внесите изменения в файлы
2. Откройте `chrome://extensions/`
3. Нажмите кнопку обновления (🔄) для расширения
4. Проверьте изменения

### Тестирование

```javascript
// В консоли расширения (background.js)
console.log('Test message');

// В консоли страницы (content.js)
console.log('Content script loaded');
```

## API

### Отправка данных на бэкенд

```javascript
// POST /api/v1/sessions/upload
const formData = new FormData();
formData.append('file_video', videoBlob, 'recording.webm');
formData.append('file_audio', audioBlob, 'audio.wav');
formData.append('file_clicks', clicksBlob, 'clicks.json');

fetch('http://localhost:8000/api/v1/sessions/upload', {
    method: 'POST',
    body: formData
});
```

## Лицензия

MIT License

---

**Вопросы?** Создайте issue на GitHub!
