# AutoDoc AI - Frontend Guide

## 🎯 Обзор

Полнофункциональный веб-интерфейс для системы автоматической генерации пошаговых гайдов из записей экрана.

## 🚀 Доступ к приложению

После запуска Docker Compose, фронтенд доступен по адресу:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

## 📱 Основные страницы

### 1. Список гайдов (/)
**Путь**: `/` или `/guides`

**Функционал**:
- Просмотр всех созданных гайдов
- Поиск по названию и описанию
- Быстрый доступ к редактированию, экспорту и удалению
- Отображение статуса обработки (draft, processing, completed)
- Счетчик шагов для каждого гайда

**Действия**:
- **Create New Guide** - создать новый гайд
- **Edit** - редактировать шаги
- **Export** - экспортировать в Markdown/HTML
- **Delete** - удалить гайд

### 2. Создание гайда (/guides/create)
**Путь**: `/guides/create`

**Функционал**:
- Заполнение информации о гайде:
  - Название (обязательно)
  - Описание
  - Язык (русский/английский)
  - Голос для озвучки (выбор из доступных TTS голосов)
- Загрузка видео записи экрана:
  - Drag & Drop или выбор файла
  - Поддержка MP4, WebM, MOV
  - Максимальный размер: 500MB
  - Прогресс загрузки в реальном времени

**Процесс**:
1. Заполните информацию о гайде
2. Загрузите видео с записью экрана
3. Система автоматически:
   - Распознает речь (ASR)
   - Извлечет скриншоты в моменты кликов
   - Сгенерирует текстовые инструкции
   - Создаст пошаговый гайд

### 3. Редактор гайда (/guides/:id/edit)
**Путь**: `/guides/{id}/edit`

**Функционал**:

#### Панель предпросмотра (слева):
- Отображение скриншота текущего шага
- Желтые маркеры на позициях кликов
- Перетаскивание маркеров мышью для корректировки позиции
- Автоматическое сохранение новых координат

#### Панель шагов (справа):
- Список всех шагов с нумерацией
- Редактирование текста каждого шага:
  - Клик на "Edit text" для редактирования
  - Ctrl+Enter для сохранения
  - Escape для отмены
- Изменение порядка шагов (стрелки вверх/вниз)
- Удаление шагов
- Автоматическое сохранение изменений

**Горячие клавиши**:
- `Ctrl+Enter` - сохранить редактируемый текст
- `Escape` - отменить редактирование

### 4. Экспорт гайда (/guides/:id/export)
**Путь**: `/guides/{id}/export`

**Функционал**:

#### Форматы экспорта:
1. **Markdown (.md)**
   - Простой текстовый формат
   - Поддержка изображений
   - Идеален для GitHub, документации

2. **HTML (.html)**
   - Готовая веб-страница
   - Стилизованный дизайн
   - Можно открыть в любом браузере

#### Возможности:
- Предпросмотр в реальном времени
- Скачивание файла
- Копирование в буфер обмена
- Переход к генерации Shorts

### 5. Генератор Shorts (/guides/:id/shorts)
**Путь**: `/guides/{id}/shorts`

**Функционал**:

#### Настройки видео:
- **Разрешение**: 1080x1920 (Full HD) или 720x1280 (HD)
- **FPS**: 30 или 60 кадров в секунду
- **Качество**: High, Medium, Low
- **Голос**: Выбор из доступных TTS голосов
- **Цвет маркера**: Настройка цвета выделения кликов
- **Размер маркера**: Настройка размера маркера

#### Процесс генерации:
1. Настройте параметры видео
2. Нажмите "Generate Shorts"
3. Отслеживайте прогресс в реальном времени:
   - Прогресс-бар
   - Текущий шаг обработки
   - Статус (Processing/Completed/Failed)
4. Скачайте готовое видео

#### Что генерируется:
- Вертикальное видео для TikTok/Reels/YouTube Shorts
- TTS озвучка для каждого шага
- Анимированные маркеры на позициях кликов
- Плавные переходы между шагами
- Синхронизация аудио и видео

## 🎨 Дизайн и UX

### Цветовая схема:
- **Primary**: Синий (#3b82f6) - основные действия
- **Success**: Зеленый (#10b981) - успешные операции
- **Warning**: Желтый (#f59e0b) - предупреждения
- **Danger**: Красный (#ef4444) - удаление, ошибки

### Компоненты:
- **Cards** - белые карточки с тенью для группировки контента
- **Buttons** - три стиля (primary, secondary, success)
- **Input Fields** - стилизованные поля ввода с фокусом
- **Markers** - желтые круглые маркеры для кликов
- **Progress Bars** - индикаторы прогресса

### Адаптивность:
- Полностью адаптивный дизайн
- Поддержка мобильных устройств
- Responsive grid layout
- Touch-friendly интерфейс

## 🔧 Технический стек

- **React 18** - UI библиотека
- **React Router 6** - маршрутизация
- **Axios** - HTTP клиент
- **Tailwind CSS** - стилизация
- **Heroicons** - иконки
- **React DnD** - drag & drop для маркеров
- **React Markdown** - рендеринг Markdown
- **Vite** - сборщик и dev server

## 📡 API Integration

Все запросы к API проксируются через Vite dev server:
- `/api/*` → `http://autodoc-ai:8000/api/*`

### Основные эндпоинты:
- `GET /api/v1/guides` - список гайдов
- `POST /api/v1/guides` - создание гайда
- `GET /api/v1/guides/{id}` - получение гайда
- `PATCH /api/v1/guides/{id}` - обновление гайда
- `DELETE /api/v1/guides/{id}` - удаление гайда
- `GET /api/v1/steps/guide/{id}` - шаги гайда
- `PATCH /api/v1/steps/{id}` - обновление шага
- `POST /api/v1/sessions/upload` - загрузка видео
- `POST /api/v1/shorts/generate/{id}` - генерация shorts

## 🎯 Пользовательский сценарий

### Полный цикл работы:

1. **Запись экрана**
   - Откройте Chrome Extension
   - Начните запись экрана
   - Говорите и кликайте по интерфейсу
   - Остановите запись

2. **Создание гайда**
   - Откройте http://localhost:3000
   - Нажмите "Create New Guide"
   - Заполните название и описание
   - Загрузите записанное видео
   - Дождитесь обработки

3. **Редактирование**
   - Откроется редактор автоматически
   - Проверьте сгенерированные шаги
   - Отредактируйте текст при необходимости
   - Скорректируйте позиции маркеров
   - Измените порядок шагов
   - Удалите ненужные шаги

4. **Экспорт**
   - Нажмите "Export Guide"
   - Выберите формат (Markdown или HTML)
   - Просмотрите preview
   - Скачайте файл или скопируйте в буфер

5. **Генерация Shorts**
   - Нажмите "Generate Shorts"
   - Настройте параметры видео
   - Запустите генерацию
   - Дождитесь завершения
   - Скачайте готовое видео

## 🐛 Troubleshooting

### Фронтенд не загружается:
```bash
# Проверьте статус контейнера
docker ps | grep autodoc-frontend

# Проверьте логи
docker logs autodoc-frontend

# Перезапустите контейнер
docker restart autodoc-frontend
```

### API запросы не работают:
```bash
# Проверьте backend
docker logs autodoc-ai

# Проверьте сеть
docker network inspect autodoc-network
```

### Ошибки при загрузке файлов:
- Проверьте размер файла (макс 500MB)
- Проверьте формат (MP4, WebM, MOV)
- Проверьте доступное место на диске

## 📝 Примечания

- Все изменения сохраняются автоматически
- Маркеры можно перетаскивать мышью
- Поддерживается горячие клавиши для быстрого редактирования
- Прогресс генерации обновляется в реальном времени
- Все данные хранятся в PostgreSQL
- Файлы хранятся в MinIO (S3-compatible storage)

## 🎉 Готово!

Теперь у вас есть полнофункциональный интерфейс для создания автоматических гайдов!

Откройте http://localhost:3000 и начните создавать свои первые гайды! 🚀
