# 📦 Руководство по установке AutoDoc AI System

## 🎯 Обзор системы

**AutoDoc AI System** — это полностью автоматизированная платформа для создания обучающих материалов из записей экрана. Система записывает ваши действия на экране, распознаёт речь, автоматически создаёт пошаговые инструкции и генерирует короткие вертикальные видео (Shorts) для социальных сетей.

### Ключевые особенности

- ✅ **Полная приватность** — все AI-модели работают локально, данные не покидают ваш сервер
- 🎥 **Автоматическая запись** — Chrome-расширение записывает экран, микрофон и клики
- 🤖 **AI-обработка** — Whisper для распознавания речи, LLM для нормализации текста
- 📝 **Редактор шагов** — визуальное редактирование с перетаскиванием маркеров
- 🎬 **Генерация Shorts** — вертикальные видео с озвучкой для TikTok/Instagram/YouTube

---

## 📋 Системные требования

### Минимальные требования

| Компонент | Требование |
|-----------|------------|
| **ОС** | Ubuntu 20.04+, Windows 10+ (с WSL2), macOS 12+ |
| **Docker** | Docker Engine 24.0+ |
| **Docker Compose** | v2.20+ |
| **RAM** | 16 GB (минимум) |
| **Диск** | 100 GB свободного места |
| **GPU** | NVIDIA GPU с 8GB+ VRAM (опционально, но рекомендуется) |

### Рекомендуемые требования

| Компонент | Требование |
|-----------|------------|
| **RAM** | 32 GB |
| **Диск** | 200 GB SSD |
| **GPU** | NVIDIA RTX 3060 или выше (12GB+ VRAM) |
| **CPU** | 8+ ядер |

### Для работы с GPU (рекомендуется)

- NVIDIA GPU с поддержкой CUDA 12.2+
- Драйверы NVIDIA версии 535+
- NVIDIA Container Toolkit

---

## 🚀 Установка

### Шаг 1: Установка Docker

#### Ubuntu/Debian

```bash
# Удалите старые версии Docker
sudo apt-get remove docker docker-engine docker.io containerd runc

# Установите зависимости
sudo apt-get update
sudo apt-get install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавьте официальный GPG ключ Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавьте репозиторий Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установите Docker Engine
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Добавьте текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Перезайдите в систему или выполните
newgrp docker

# Проверьте установку
docker --version
docker compose version
```

#### Windows (с WSL2)

1. Установите [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)
2. Включите WSL2 backend в настройках Docker Desktop
3. Установите Ubuntu из Microsoft Store
4. Откройте Ubuntu и выполните команды из раздела Ubuntu выше

#### macOS

```bash
# Установите Docker Desktop for Mac
# Скачайте с https://www.docker.com/products/docker-desktop/

# Или через Homebrew
brew install --cask docker

# Запустите Docker Desktop
open -a Docker

# Проверьте установку
docker --version
docker compose version
```

### Шаг 2: Установка NVIDIA Container Toolkit (для GPU)

**Только для систем с NVIDIA GPU!**

#### Ubuntu/Debian

```bash
# Установите драйверы NVIDIA (если ещё не установлены)
sudo apt-get install -y nvidia-driver-535

# Перезагрузите систему
sudo reboot

# Проверьте драйверы
nvidia-smi

# Установите NVIDIA Container Toolkit
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | \
  sudo tee /etc/apt/sources.list.d/nvidia-docker.list

sudo apt-get update
sudo apt-get install -y nvidia-container-toolkit

# Перезапустите Docker
sudo systemctl restart docker

# Проверьте GPU в Docker
docker run --rm --gpus all nvidia/cuda:12.2.0-base-ubuntu22.04 nvidia-smi
```

### Шаг 3: Клонирование репозитория

```bash
# Клонируйте проект
git clone https://github.com/your-org/autodoc-ai.git
cd autodoc-ai

# Создайте директории для данных
mkdir -p data/models data/uploads data/output data/screenshots
```

### Шаг 4: Настройка окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл
nano .env
```

#### Основные параметры в `.env`

```bash
# === Базовые настройки ===
APP_NAME=AutoDoc AI System
DEBUG=true
ENVIRONMENT=development

# === База данных PostgreSQL ===
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=autodoc
DATABASE_PASSWORD=ИЗМЕНИТЕ_ЭТОТ_ПАРОЛЬ  # ⚠️ ВАЖНО: Смените пароль!
DATABASE_NAME=autodoc_db

# === Redis ===
REDIS_HOST=redis
REDIS_PORT=6379

# === MinIO (хранилище файлов) ===
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=ИЗМЕНИТЕ_ЭТОТ_ПАРОЛЬ  # ⚠️ ВАЖНО: Смените пароль!

# === AI Модели (локальные) ===
# Whisper для распознавания речи
WHISPER_MODEL_SIZE=medium  # tiny/base/small/medium/large
WHISPER_DEVICE=cuda        # cuda или cpu

# LLM для нормализации текста (локальная модель)
LLM_MODEL_NAME=Qwen/Qwen2.5-7B-Instruct-GGUF
LLM_MODEL_PATH=/data/models/Qwen2.5-7B-Instruct-Q4_0.gguf

# TTS для озвучки
TTS_ENGINE=edge-tts
EDGE_TTS_VOICE=ru-RU-SvetlanaNeural

# === GPU настройки ===
GPU_DEVICE_ID=0
GPU_MEMORY_FRACTION=0.8
```

### Шаг 5: Запуск системы

```bash
# Запустите все сервисы
docker compose up --build -d

# Просмотр логов
docker compose logs -f

# Проверьте статус контейнеров
docker compose ps
```

**Первый запуск может занять 10-15 минут**, так как Docker скачивает образы и AI-модели.

### Шаг 6: Проверка работоспособности

```bash
# Проверьте здоровье API
curl http://localhost:8000/health

# Ожидаемый ответ:
# {
#   "status": "healthy",
#   "version": "1.0.0",
#   "database": "healthy",
#   "redis": "healthy",
#   "minio": "healthy",
#   "gpu_available": true
# }
```

### Шаг 7: Доступ к сервисам

Откройте в браузере:

| Сервис | URL | Учётные данные |
|--------|-----|----------------|
| **Frontend (Web UI)** | http://localhost:3000 | — |
| **Backend API** | http://localhost:8000 | — |
| **API Документация** | http://localhost:8000/docs | — |
| **MinIO Console** | http://localhost:9001 | minioadmin / minioadmin |
| **pgAdmin** | http://localhost:5050 | admin@example.com / admin |

---

## 🔧 Установка Chrome Extension

### Шаг 1: Подготовка расширения

```bash
# Расширение уже находится в папке extension/
cd extension

# Создайте иконки (если их нет)
python generate_icons.py

# Проверьте файлы
ls -la
# Должны быть файлы: manifest.json, background.js, content.js, popup.html, popup.js
# И папка icons/ с файлами: icon16.png, icon48.png, icon128.png
```

### Шаг 2: Загрузка в Chrome

1. Откройте Chrome и перейдите на `chrome://extensions/`
2. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
3. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
4. Выберите папку `extension/` из проекта
5. Расширение появится в списке
6. Закрепите расширение на панели инструментов (нажмите на иконку пазла и закрепите)

### Шаг 3: Настройка расширения

1. Нажмите на иконку расширения AutoDoc AI
2. В настройках укажите URL бэкенда: `http://localhost:8000`
3. Нажмите "Сохранить"

---

## 🎯 Первый запуск и тестирование

### Тест 1: Проверка API

```bash
# Проверьте здоровье системы
curl http://localhost:8000/health

# Получите список гайдов (должен быть пустым)
curl http://localhost:8000/api/v1/guides
```

### Тест 2: Создание тестового гайда

1. Откройте http://localhost:3000
2. Нажмите **"Create New Guide"**
3. Введите название: "Тестовый гайд"
4. Нажмите **"Create"**
5. Гайд должен появиться в списке

### Тест 3: Запись экрана

1. Нажмите на иконку расширения AutoDoc AI
2. Нажмите **"Начать запись"**
3. Разрешите доступ к экрану и микрофону
4. Выполните несколько действий (клики, ввод текста)
5. Говорите вслух что делаете: "Сейчас я нажму на кнопку..."
6. Нажмите **"Остановить запись"**
7. Расширение автоматически отправит данные на сервер

### Тест 4: Проверка обработки

1. Откройте http://localhost:3000
2. Найдите новый гайд в списке (статус "Processing")
3. Дождитесь завершения обработки (статус изменится на "Draft")
4. Откройте гайд и проверьте созданные шаги

---

## 🛠️ Управление системой

### Основные команды Docker Compose

```bash
# Запуск всех сервисов
docker compose up -d

# Остановка всех сервисов
docker compose down

# Перезапуск конкретного сервиса
docker compose restart autodoc-ai

# Просмотр логов
docker compose logs -f

# Просмотр логов конкретного сервиса
docker compose logs -f autodoc-ai
docker compose logs -f celery-worker

# Проверка статуса
docker compose ps

# Пересборка образов
docker compose build --no-cache

# Полная перезагрузка
docker compose down -v  # ⚠️ Удалит все данные!
docker compose up --build -d
```

### Мониторинг ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Использование GPU
nvidia-smi

# Использование диска
docker system df

# Очистка неиспользуемых данных
docker system prune -a
```

---

## 🔍 Решение проблем

### Проблема: Docker не запускается

```bash
# Проверьте статус Docker
sudo systemctl status docker

# Перезапустите Docker
sudo systemctl restart docker

# Проверьте логи
sudo journalctl -u docker.service
```

### Проблема: GPU не обнаружен

```bash
# Проверьте драйверы NVIDIA
nvidia-smi

# Проверьте NVIDIA Container Toolkit
docker run --rm --gpus all nvidia/cuda:12.2.0-base-ubuntu22.04 nvidia-smi

# Переустановите NVIDIA Container Toolkit
sudo apt-get install --reinstall nvidia-container-toolkit
sudo systemctl restart docker
```

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker compose logs

# Проверьте порты
sudo netstat -tulpn | grep -E '3000|8000|5432|6379|9000'

# Освободите порты если заняты
sudo lsof -ti:8000 | xargs kill -9
```

### Проблема: База данных не подключается

```bash
# Проверьте контейнер PostgreSQL
docker compose logs postgres

# Подключитесь к базе вручную
docker exec -it autodoc-postgres psql -U autodoc -d autodoc_db

# Пересоздайте базу
docker compose down -v
docker compose up -d postgres
```

### Проблема: MinIO не доступен

```bash
# Проверьте контейнер MinIO
docker compose logs minio

# Проверьте бакеты
docker exec -it autodoc-minio mc ls myminio/

# Пересоздайте бакеты
docker compose up minio-client
```

### Проблема: Модели не скачиваются

```bash
# Проверьте место на диске
df -h

# Скачайте модели вручную
mkdir -p data/models
cd data/models

# Whisper скачается автоматически при первом использовании

# LLM модель (Qwen 2.5 7B)
wget https://huggingface.co/Qwen/Qwen2.5-7B-Instruct-GGUF/resolve/main/qwen2.5-7b-instruct-q4_0.gguf \
  -O Qwen2.5-7B-Instruct-Q4_0.gguf
```

### Проблема: Frontend не открывается

```bash
# Проверьте контейнер
docker compose logs autodoc-frontend

# Перезапустите
docker compose restart autodoc-frontend

# Пересоберите
docker compose build autodoc-frontend
docker compose up -d autodoc-frontend
```

### Проблема: Расширение не записывает

1. Проверьте разрешения Chrome на доступ к микрофону и экрану
2. Откройте `chrome://extensions/` и проверьте что расширение включено
3. Проверьте консоль расширения (Inspect views → service worker)
4. Перезагрузите расширение

---

## 📊 Проверка производительности

### Тест скорости обработки

```bash
# Создайте тестовую запись длительностью 1 минута
# Ожидаемое время обработки:
# - ASR (Whisper): 30-60 секунд
# - Step Detection: 10-20 секунд
# - LLM Normalization: 20-40 секунд (зависит от количества шагов)
# - Screenshot Extraction: 10-20 секунд
# Итого: 1-2 минуты для 1 минуты записи
```

### Мониторинг GPU

```bash
# Запустите мониторинг в реальном времени
watch -n 1 nvidia-smi

# Проверьте использование VRAM
nvidia-smi --query-gpu=memory.used,memory.total --format=csv
```

---

## 🔒 Безопасность

### Рекомендации для продакшена

1. **Смените все пароли по умолчанию** в `.env`
2. **Используйте HTTPS** для доступа к API
3. **Настройте firewall** для ограничения доступа
4. **Включите аутентификацию** в API
5. **Регулярно обновляйте** Docker образы

### Настройка HTTPS (опционально)

```bash
# Установите Nginx
sudo apt-get install nginx

# Получите SSL сертификат (Let's Encrypt)
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com

# Настройте проксирование в Nginx
sudo nano /etc/nginx/sites-available/autodoc
```

Пример конфигурации Nginx:

```nginx
server {
    listen 443 ssl http2;
    server_name yourdomain.com;
    
    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
    }
}
```

---

## 📚 Дополнительные ресурсы

- **Полная документация**: [README.md](README.md)
- **Руководство пользователя**: [USER_GUIDE_RU.md](USER_GUIDE_RU.md)
- **API документация**: http://localhost:8000/docs
- **Быстрый старт**: [QUICK_START.md](QUICK_START.md)

---

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Просмотрите логи: `docker compose logs -f`
3. Создайте issue на GitHub с описанием проблемы и логами

---

**Готово! Система установлена и готова к работе! 🎉**

Следующий шаг: [Руководство пользователя](USER_GUIDE_RU.md)
