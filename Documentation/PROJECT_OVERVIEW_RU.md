# 🎯 Обзор проекта AutoDoc AI System

## Что это такое?

**AutoDoc AI System** — это полностью автоматизированная платформа для создания обучающих материалов из записей экрана. Аналог Guidde.com, но с полной приватностью — все AI-модели работают локально на вашем сервере.

## Основная идея

```
Вы записываете → Говорите → Кликаете
         ↓
Система автоматически создаёт:
✅ Пошаговые инструкции с скриншотами
✅ Короткие видео (Shorts) с озвучкой
✅ Markdown/HTML документацию
```

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                  AutoDoc AI System                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Chrome Extension → FastAPI Backend → AI Processing    │
│  (Запись)           (API)              (Whisper+LLM)   │
│                                                         │
│  React Frontend ← PostgreSQL ← MinIO Storage           │
│  (Редактор)       (БД)         (Файлы)                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## Технологический стек

### Backend
- **FastAPI** — веб-фреймворк
- **PostgreSQL** — база данных
- **Redis + Celery** — очередь задач
- **MinIO** — S3-совместимое хранилище

### Frontend
- **React** — UI библиотека
- **Vite** — сборщик
- **TailwindCSS** — стили
- **Axios** — HTTP клиент

### AI/ML (локальные модели)
- **Whisper** (medium) — распознавание речи
- **Qwen 2.5 7B** (GGUF) — нормализация текста
- **Edge TTS** — озвучка
- **FFmpeg** — обработка видео

### Browser Extension
- **Chrome Extension Manifest V3**
- **MediaRecorder API** — запись экрана
- **WebRTC** — захват аудио

## Основной флоу работы

### 1. Запись (Chrome Extension)

```javascript
Пользователь нажимает "Начать запись"
  ↓
Расширение записывает:
  • Видео экрана (WebM)
  • Аудио с микрофона (WAV)
  • Лог кликов с координатами (JSON)
  ↓
Автоматически отправляет на бэкенд
```

### 2. Обработка (Backend + AI)

```python
Celery Worker получает задачу
  ↓
1. ASR (Whisper): аудио → текст + таймкоды
2. Step Detection: клики + речь → шаги
3. Screenshot Extraction: FFmpeg извлекает кадры
4. LLM Normalization: "эм, нужно нажать" → "Нажмите кнопку"
  ↓
Создаёт гайд со статусом "Draft"
```

### 3. Редактирование (Web UI)

```
Пользователь открывает гайд
  ↓
Видит:
  • Скриншоты с маркерами кликов
  • Нормализованные инструкции
  • Оригинальную речь (для сравнения)
  ↓
Может:
  • Редактировать текст
  • Перетаскивать маркеры
  • Менять порядок шагов
  • Удалять/объединять шаги
  ↓
Всё сохраняется автоматически
```

### 4. Генерация Shorts (AI + FFmpeg)

```python
Пользователь нажимает "Generate Shorts"
  ↓
1. TTS: текст каждого шага → аудио
2. Video Composition:
   • Скриншот + маркер + озвучка
   • Вертикальный формат 1080×1920
   • Плавные переходы
3. Rendering: склейка всех шагов
  ↓
Готовое видео для TikTok/Instagram/YouTube
```

## Структура базы данных

```sql
RecordingSession (сессия записи)
  ├── id, uuid, status
  ├── video_path, audio_path, clicks_log_path
  ├── asr_text, asr_segments
  └── Guide (гайд) ──┐
                     │
Guide (гайд)         │
  ├── id, uuid, title, status
  ├── tts_voice, language
  ├── shorts_video_path
  └── GuideStep[] (шаги) ──┐
                            │
GuideStep (шаг)             │
  ├── step_number
  ├── click_timestamp, click_x, click_y
  ├── screenshot_path
  ├── raw_speech, normalized_text, edited_text
  └── tts_audio_path, tts_duration
```

## API Endpoints

### Сессии
```
POST   /api/v1/sessions/upload      # Загрузить запись
GET    /api/v1/sessions              # Список сессий
GET    /api/v1/sessions/{id}         # Получить сессию
DELETE /api/v1/sessions/{id}         # Удалить сессию
```

### Гайды
```
POST   /api/v1/guides                # Создать гайд
GET    /api/v1/guides                # Список гайдов
GET    /api/v1/guides/{id}           # Получить гайд
PATCH  /api/v1/guides/{id}           # Обновить гайд
DELETE /api/v1/guides/{id}           # Удалить гайд
```

### Шаги
```
GET    /api/v1/guides/{id}/steps     # Список шагов
PATCH  /api/v1/steps/{id}            # Обновить шаг
DELETE /api/v1/steps/{id}            # Удалить шаг
POST   /api/v1/steps/merge           # Объединить шаги
```

### Экспорт
```
GET    /api/v1/guides/{id}/export/markdown  # Экспорт в MD
GET    /api/v1/guides/{id}/export/html      # Экспорт в HTML
```

### Shorts
```
POST   /api/v1/guides/{id}/shorts/generate  # Генерация Shorts
GET    /api/v1/shorts/{task_id}/status      # Статус генерации
GET    /api/v1/shorts/{task_id}/download    # Скачать видео
```

## Файловая структура

```
autodoc_ai/
├── app/                          # Backend приложение
│   ├── api/                      # API endpoints
│   │   ├── sessions.py           # Сессии записи
│   │   ├── guides.py             # CRUD гайдов
│   │   ├── steps.py              # Редактирование шагов
│   │   ├── export.py             # Экспорт
│   │   └── shorts.py             # Генерация Shorts
│   ├── services/                 # Бизнес-логика
│   │   ├── asr_service.py        # Whisper ASR
│   │   ├── llm_service.py        # LLM нормализация
│   │   ├── step_detector.py      # Выделение шагов
│   │   ├── screenshot_service.py # Извлечение скриншотов
│   │   ├── tts_service.py        # TTS озвучка
│   │   ├── shorts_generator.py   # Сборка Shorts
│   │   └── storage.py            # MinIO хранилище
│   ├── models.py                 # SQLAlchemy модели
│   ├── schemas.py                # Pydantic схемы
│   ├── config.py                 # Конфигурация
│   ├── database.py               # Подключение к БД
│   ├── celery.py                 # Celery конфигурация
│   ├── celery_tasks.py           # Celery задачи
│   └── main.py                   # FastAPI приложение
│
├── frontend/                     # React приложение
│   ├── src/
│   │   ├── pages/                # Страницы
│   │   │   ├── Dashboard.jsx     # Главная
│   │   │   ├── StepEditor.jsx    # Редактор шагов
│   │   │   ├── ShortsPreview.jsx # Просмотр Shorts
│   │   │   └── SessionStatus.jsx # Статус обработки
│   │   ├── components/           # Компоненты
│   │   │   ├── Navbar.jsx
│   │   │   ├── GuideCard.jsx
│   │   │   └── StepCard.jsx
│   │   ├── services/
│   │   │   └── api.js            # API клиент
│   │   └── App.jsx               # Главный компонент
│   ├── package.json
│   └── vite.config.js
│
├── extension/                    # Chrome Extension
│   ├── manifest.json             # Манифест расширения
│   ├── background.js             # Service Worker
│   ├── content.js                # Content Script
│   ├── popup.html                # UI popup
│   └── popup.js                  # Логика popup
│
├── data/                         # Локальные данные
│   ├── models/                   # AI модели (GGUF)
│   ├── uploads/                  # Загруженные сессии
│   ├── screenshots/              # Извлечённые скриншоты
│   └── output/                   # Готовые Shorts
│
├── docker-compose.yml            # Docker Compose
├── Dockerfile                    # Docker образ
├── requirements.txt              # Python зависимости
├── .env.example                  # Пример конфигурации
│
└── docs/                         # Документация
    ├── INSTALLATION_GUIDE_RU.md  # Установка
    ├── USER_GUIDE_RU.md          # Руководство пользователя
    └── PROJECT_OVERVIEW_RU.md    # Этот файл
```

## Ключевые особенности

### 1. Полная приватность

```
✅ Все AI-модели работают локально
✅ Данные не покидают ваш сервер
✅ Нет внешних API (OpenAI, Anthropic)
✅ Полный контроль над данными
```

### 2. Автоматизация

```
✅ Автоматическое распознавание речи
✅ Автоматическое создание шагов
✅ Автоматическая нормализация текста
✅ Автоматическое извлечение скриншотов
```

### 3. Гибкость

```
✅ Редактирование всех элементов
✅ Перетаскивание маркеров
✅ Изменение порядка шагов
✅ Объединение/разделение шагов
```

### 4. Множество форматов

```
✅ Markdown для документации
✅ HTML для веб-страниц
✅ Shorts для соцсетей
✅ JSON для интеграций
```

## Производительность

### Время обработки (для 1 минуты записи)

| Этап | Время | Требования |
|------|-------|------------|
| ASR (Whisper) | 30-60 сек | GPU: 8GB VRAM |
| Step Detection | 10-20 сек | CPU |
| Screenshot Extraction | 10-20 сек | CPU |
| LLM Normalization | 20-40 сек | GPU: 8GB VRAM |
| **Итого** | **1-2 минуты** | |

### Генерация Shorts (для 5 шагов)

| Этап | Время | Требования |
|------|-------|------------|
| TTS Generation | 30-60 сек | CPU/GPU |
| Video Composition | 1-2 мин | CPU + FFmpeg |
| Rendering | 30-60 сек | CPU |
| **Итого** | **2-4 минуты** | |

## Системные требования

### Минимальные
- 16GB RAM
- 100GB SSD
- 8GB VRAM (опционально)

### Рекомендуемые
- 32GB RAM
- 200GB SSD
- NVIDIA RTX 3060+ (12GB VRAM)

## Масштабирование

### Горизонтальное

```bash
# Увеличить количество воркеров
docker compose up -d --scale celery-worker=4
```

### Вертикальное

```bash
# Увеличить ресурсы контейнеров
# В docker-compose.yml:
services:
  celery-worker:
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 16G
```

## Безопасность

### Рекомендации

1. **Смените пароли** в `.env`
2. **Используйте HTTPS** для продакшена
3. **Настройте firewall**
4. **Включите аутентификацию**
5. **Регулярно обновляйте** зависимости

## Лицензия

MIT License — свободное использование для любых целей.

---

## Быстрые ссылки

- 📦 [Установка](INSTALLATION_GUIDE_RU.md)
- 📖 [Руководство пользователя](USER_GUIDE_RU.md)
- 🚀 [Быстрый старт](QUICK_START.md)
- 📚 [API документация](http://localhost:8000/docs)
- 🐛 [GitHub Issues](https://github.com/your-org/autodoc-ai/issues)

---

**AutoDoc AI System** — превращайте записи экрана в профессиональные гайды за минуты! 🚀
