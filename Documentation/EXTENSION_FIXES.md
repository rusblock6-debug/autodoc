# 🔧 Исправления Chrome Extension

## Последнее обновление

### Проблема: "Cannot read properties of undefined (reading 'create')"

**Причина:** `chrome.notifications.create` требует разрешение `notifications` в manifest.json

**Решение:**
1. Добавлено разрешение `notifications` и `tabs` в manifest.json
2. Обёрнут вызов notifications в try-catch
3. Упрощён код background.js

---

## Проблемы и решения

### Проблема 1: Запись прекращается при закрытии popup

**Причина:** MediaRecorder создавался в popup.js, который уничтожается при закрытии popup.

**Решение:** Перенёс всю логику записи в background.js (Service Worker), который работает постоянно.

### Проблема 2: {"detail":"Not Found"} при открытии редактора

**Причина:** 
1. Неправильный URL endpoint (двойной prefix `/sessions/sessions/`)
2. API ожидал обязательные файлы video и audio
3. Frontend использовал неправильные роуты

**Решение:**
1. Убрал дублирующийся prefix в `app/api/sessions.py`
2. Сделал все файлы опциональными в API
3. Добавил роут `/sessions/:sessionId` в App.jsx
4. Обновил API для работы только с логом кликов

### Проблема 3: Клики не записывались

**Причина:** Content script не получал сообщение о начале записи.

**Решение:** Обновил content.js для корректной обработки сообщений и добавил визуальную обратную связь при кликах.

## Изменённые файлы

### extension/background.js
- Полностью переписан
- Вся логика записи теперь здесь
- Отправка данных на бэкенд
- Управление состоянием записи

### extension/popup.js
- Упрощён до UI логики
- Запрашивает screen capture
- Отправляет команды в background.js
- Отображает статус записи

### extension/content.js
- Улучшено логирование кликов
- Добавлена визуальная обратная связь (жёлтый маркер)
- Корректная обработка сообщений

### extension/popup.html
- Переведён на русский язык

### app/api/sessions.py
- Убран дублирующийся prefix
- Все файлы теперь опциональны
- Добавлен endpoint для списка сессий
- Упрощена логика создания гайда

### frontend/src/App.jsx
- Добавлен роут `/sessions/:sessionId`

### frontend/src/services/api.js
- Добавлен метод `sessionsApi.getAll()`
- Добавлен метод `guidesApi.getByUuid()`

### frontend/src/pages/StepEditor.jsx
- Обновлён для работы с UUID

## Как проверить

1. Перезагрузите расширение в Chrome:
   - Откройте `chrome://extensions/`
   - Нажмите кнопку обновления на расширении

2. Перезапустите бэкенд:
   ```bash
   docker compose restart autodoc-ai
   ```

3. Проверьте работу:
   - Нажмите "Начать запись"
   - Выберите экран для захвата
   - Кликайте по элементам (должны появляться жёлтые маркеры)
   - Нажмите "Остановить"
   - Должен открыться Dashboard с новым гайдом

## Логи для отладки

В консоли расширения (background.js):
```
[AutoDoc] Background script loaded
[AutoDoc] Message received: START_RECORDING
[AutoDoc] Click logged: 450, 320 at 5.234s
[AutoDoc] Message received: STOP_RECORDING
[AutoDoc] Upload success: {session_id: "..."}
```

В консоли страницы (content.js):
```
[AutoDoc Content] Script loaded on: https://...
[AutoDoc Content] Recording STARTED
[AutoDoc Content] Click #1: 450 320 BUTTON
[AutoDoc Content] Recording STOPPED, clicks: 5
```

## Известные ограничения

1. **Без видео/аудио записи** — текущая версия записывает только клики
2. **Без AI обработки** — шаги создаются напрямую из кликов
3. **Без скриншотов** — нужна интеграция с FFmpeg для извлечения кадров

## Следующие шаги

1. Добавить запись видео через MediaRecorder в background.js
2. Интегрировать Whisper для распознавания речи
3. Добавить извлечение скриншотов через FFmpeg
4. Интегрировать LLM для нормализации текста
